version: '3.8'

services:
  strom-raw-dump:
    build: .
    container_name: strom-raw-dump
    devices:
      - "/dev/ttyUSB0:/dev/ttyUSB0"
    volumes:
      - ./logs:/app/logs
    environment:
      - DEBUG=1
    restart: unless-stopped
    tty: true
