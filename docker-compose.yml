services:

  strom-raw-dump:
    build: ./reader
    container_name: strom-reader
    devices:
      - "/dev/ttyUSB0:/dev/ttyUSB0"
    volumes:
      - ./logs:/app/logs
      - /var/www/html:/app/data
    environment:
      - DEBUG=1
    restart: unless-stopped
    tty: true
    
  strom-dashboard:
    build: ./lighttpd
    container_name: strom-dashboard
    ports:
      - "8080:80"
    volumes:
      - /var/www/html:/var/www/localhost/htdocs:ro
