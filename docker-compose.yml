services:
  strom-raw-dump:
    build: .
    container_name: strom-raw-dump
    devices:
      - "/dev/ttyUSB0:/dev/ttyUSB0"
    volumes:
      - ./logs:/app/logs
      - /var/www/html:/app/data
    environment:
      - DEBUG=0
    restart: unless-stopped
    tty: true
